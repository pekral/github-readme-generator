{
  "name": "@acme/slugify",
  "version": "2.1.0",
  "description": "Turns arbitrary text into URL-safe slugs.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test",
    "lint": "eslint src"
  }
}
